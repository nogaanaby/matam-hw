#include "customer.h"

// create array of cluint with stdin input M , set the fildes to zero 
struct Customer* createCustomer(){
    // to do 
}

// add a new Customer to array of Customer = > 1.check for this Customer in data 2.find the pleace in array 
// 3. return error if in array 
 struct Customer* addNewCustomer( struct Customer* c){
     // to do 
 }

// sarch for Customer in data base 
 struct CarCustomers* SarchCustomer( struct Customer* C){
     // to do 
 }

// delte Customer by id number
void deleteCustomer(char* Customer_id){
    // to do 
}

// delete all Customer 
void deleteAllCustomer(){
    // to do 
}

