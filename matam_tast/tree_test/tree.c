#include "tree.h"

Node* create_binary_tree(){

} 


int isOrderTree(Node* head){

}

void insert_to_tree(Node* head,Node* new_node){
    /*stop: if you get to null then this is the pleace to put the new node  */
    if(!head){
        return new_node; 
    }
    /* if not we need to check if go right or left */
    if()

}

void print_tree(Node* haed){

} 


int main(){
    Node *head ;
    head = create_binary_tree();
    isOrderTree(head);
    return 0 ; 
}